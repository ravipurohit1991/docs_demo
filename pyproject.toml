[build-system]
requires = ["flit_core >=3.2,<4"]
build-backend = "flit_core.buildapi"

[project]
name = "lauetoolsnn"
authors = [{name = "Ravi PURUSHOTTAM", email = "purushot@esrf.fr"}]
dynamic = ["version", "description"]
